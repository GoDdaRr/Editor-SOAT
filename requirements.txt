# Dependencias principales del proyecto Editor SOAT

# Flask y servidor de producción
Flask==2.3.3
Werkzeug==2.3.7
gunicorn==21.2.0

# Procesamiento de imágenes
opencv-python-headless==4.8.1.78
Pillow==10.0.1
numpy==1.26.4

# Conversión de PDFs (requiere Poppler instalado en el sistema)
pdf2image==1.16.3

# OCR para detección automática del identificador (placa).
# Requiere el binario de Tesseract instalado en el sistema.
pytesseract==0.3.10
